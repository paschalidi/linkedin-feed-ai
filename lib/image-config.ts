/**
 * Image dimensions for the branded post image (LinkedIn portrait post — 4:5).
 * Both the generator and the preview component read from this file.
 */
export const IMAGE_WIDTH = 1080;
export const IMAGE_HEIGHT = 1350;
